// Backbone model for the Life Events
var LifeEventModel = Backbone.Model.extend({

  // each life event would have
  // time, event
  initialize: function(){
    //this.set('signedin', false);
    //this.set('username', null);
  }
  
});
